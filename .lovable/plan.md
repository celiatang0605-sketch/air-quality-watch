## Plan: 统一调整「无烟排行榜」与「改进建议榜」排名颜色样式

### 目标
仅修改 `RankPage` 中排名数字/徽标的颜色与展示样式，两个榜单共用卡片结构，根据榜单类型切换配色体系。不改动页面结构、数据逻辑、排序逻辑、路由和交互。

### 修改范围
`src/pages/Index.tsx` 中 `RankPage` 组件的排名渲染部分（当前第 1761–1766 行左右）。

### 具体改动

#### 1. 引入颜色映射常量
在 `RankPage` 作用域内定义两个榜单的 rank 颜色配置：

- **clean（绿色体系）**
  - 第 1 名：背景 `#2F9E5B`，文字白色
  - 第 2 名：背景 `#4CAF70`，文字白色
  - 第 3 名：背景 `#DFF5E8`，文字 `#3FA463`
  - 第 4 名及之后：无背景色块，文字 `#2F9E5B`，加粗

- **improve（橙色体系）**
  - 第 1 名：背景 `#F08A24`，文字白色
  - 第 2 名：背景 `#F3A64B`，文字白色
  - 第 3 名：背景 `#F7C98A`，文字 `#9A520D`
  - 第 4 名及之后：无背景色块，文字 `#111111` 或 `#222222`，加粗，左对齐

#### 2. 更新排名渲染 JSX
将当前单一的 `w-8 h-8 rounded-lg` 徽标替换为按 `tab` 与 `index` 条件渲染：
- 第 1–3 名：保留圆形/圆角背景块，使用对应榜单配色。
- 第 4 名及之后：移除背景块，仅渲染加粗数字文本，颜色按榜单切换。
- 确保数字与左侧内容对齐整齐，卡片间距不变。

#### 3. 移动端检查
完成后验证预览，确保：
- 排名数字字号、字重清晰
- 卡片无横向滚动
- 图片、文字、排名区域对齐正常

### 保留内容（完全不变）
- 顶部标题、城市榜单头图、榜单切换 tab
- 场所图片、名称、评分标签、反馈标签、距离信息
- 收藏按钮、底部导航栏、发布按钮
- 数据筛选逻辑、排序逻辑、路由与交互流程
